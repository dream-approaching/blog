import React from 'react';

function App() {
  const style = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh'
  };
  return (
    <div style={style}>
      请运行<code>npm run storybook</code>查看
    </div>
  );
}

export default App;
